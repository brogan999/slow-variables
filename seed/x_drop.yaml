# Weekly manual pass (v2 §5.4): drop post URLs here; `ingest x_drop` fetches each through X's oEmbed endpoint and
# records a tier-7 `watch.x.<handle>.pt` row (never a status). Links in a post to a tier 1-6 artifact are listed on
# the row so the artifact can become a manual observation.
tweets: []
